import { QuestTypePipe } from './quest-type.pipe';

describe('QuestTypePipe', () => {
  it('create an instance', () => {
    const pipe = new QuestTypePipe();
    expect(pipe).toBeTruthy();
  });
});
